type Product {
    _id: ID!
    name: String!
    description: String!
    createdAt: String!
    updatedAt: String!
    created_by: User
}

type Query {
    products: [Product]
    product(id: ID!): Product
}