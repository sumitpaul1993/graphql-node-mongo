type User {
    _id: ID!
    name: String!
    email: String!
    createdAt: String!
    updatedAt: String!
}

type Query {
    users: [User]
}

type Mutation {
    addUser(name: String!, email: String!) : String
}